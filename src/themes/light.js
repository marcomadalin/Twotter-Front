import { createTheme } from "@mui/material";

const themeLight = createTheme({
  palette: {
    primary: {
      main: "#1d9bf0",
    },
    background: {
      default: "#e4f0e2",
    },
  },
});

export default themeLight;
